# РУКОВОДСТВО ПО ЗАПУСКУ ПРОЕКТА

## ⚠️ ВАЖНО: Проблема с Python 3.13

**Проект несовместим с Python 3.13!**

Используйте **Python 3.11 или 3.12**.

Подробно читайте: [`PYTHON-313-FIX.md`](PYTHON-313-FIX.md)

---

## 🚀 БЫСТРЫЙ СТАРТ

### Windows (PowerShell) - РЕКОМЕНДУЕТСЯ

```powershell
# 1. Установить Python 3.11 (если его нет)
winget install Python.Python.3.11

# 2. После установки перезагрузить PowerShell

# 3. Создать виртуальное окружение
cd c:\Kursach\Varyusha
python3.11 -m venv venv

# 4. Активировать окружение
.\venv\Scripts\Activate.ps1

# 5. Установить зависимости
pip install -r requirements.txt

# 6. Запустить приложение
python run.py

# 7. Открыть в браузере
# http://localhost:5000
```

### Linux / macOS

```bash
# 1. Установить Python 3.11 (если его нет)
# Ubuntu: sudo apt install python3.11
# macOS: brew install python@3.11

# 2. Создать виртуальное окружение
python3.11 -m venv venv

# 3. Активировать окружение
source venv/bin/activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Запустить приложение
python run.py

# 6. Открыть в браузере
# http://localhost:5000
```

### Docker (если не хотите устанавливать Python)

```bash
docker build -t version-tracker .
docker run -p 5000:5000 version-tracker
# http://localhost:5000
```

---

## 📋 ЧТО ДЕЛАТЬ ПОСЛЕ ЗАПУСКА

### Тестирование веб-интерфейса

1. **Главная страница** http://localhost:5000
   - Просмотрите статистику
   - Закажите недавние обновления

2. **Управление проектами** http://localhost:5000/projects
   - Нажмите "Добавить проект"
   - Заполните форму:
     - Имя: `Flask`
     - Описание: `Python web framework`
     - GitHub repo: `pallets/flask`
     - PyPI package: `flask`
   - Нажмите "Добавить"

3. **Проверка обновлений**
   - Нажмите кнопку "Проверить обновление" для проекта
   - Система автоматически проверит GitHub и PyPI
   - Обновления отобразятся на странице проекта

4. **Просмотр деталей проекта**
   - Нажмите на название проекта
   - Перейдите на вкладки "Версии" и "Обновления"

### Тестирование API

```bash
# Получить все проекты
curl http://localhost:5000/api/projects

# Получить статистику
curl http://localhost:5000/api/statistics

# Получить непрочитанные уведомления
curl http://localhost:5000/api/notifications/unread

# Проверить здоровье приложения
curl http://localhost:5000/health
```

Подробнее в [`API_DOCS.md`](API_DOCS.md)

### Запуск тестов

```bash
# Убедитесь, что виртуальное окружение активировано
# Затем запустите:

pytest tests/ -v

# Запуск с отчётом о покрытии кода:
pytest tests/ --cov=. --cov-report=html
```

Результаты будут в папке `htmlcov/index.html`

---

## 📚 ДОКУМЕНТАЦИЯ

- **[README.md](README.md)** - Описание проекта
- **[INSTALL.md](INSTALL.md)** - Подробная установка и развёртывание
- **[API_DOCS.md](API_DOCS.md)** - Документация REST API
- **[ARCHITECTURE.md](ARCHITECTURE.md)** - Архитектура системы
- **[TESTING.md](TESTING.md)** - Описание тестов
- **[PYTHON-313-FIX.md](PYTHON-313-FIX.md)** - Решение проблемы Python 3.13

---

## ❌ ТИПИЧНЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка: "python: command not found"

**Решение:** Используйте `python3.11` вместо `python`

```powershell
python3.11 -m venv venv
python3.11 run.py
```

### Ошибка: "AssertionError ... super(Generic, cls)"

**Решение:** Это ошибка Python 3.13. Установите Python 3.11 или 3.12

Читайте [`PYTHON-313-FIX.md`](PYTHON-313-FIX.md)

### Ошибка: "ModuleNotFoundError: No module named 'flask'"

**Решение:** Активируйте виртуальное окружение и переустановите зависимости

```bash
# Windows
.\venv\Scripts\Activate.ps1

# Linux/macOS
source venv/bin/activate

# Затем
pip install -r requirements.txt
```

### Ошибка: "Address already in use"

**Решение:** Приложение уже запущено на порту 5000 или другой процесс его занял

```powershell
# Windows: Найти процесс на порту 5000
Get-NetTCPConnection -LocalPort 5000

# Убить процесс (если нужно)
Stop-Process -Name python -Force
```

### Ошибка при подключении к GitHub API

**Решение:** Добавьте GitHub token в `.env`

```bash
cp .env.example .env
# Отредактируйте .env и добавьте свой GitHub token:
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
```

Получить token: https://github.com/settings/tokens

---

## 🧪 ПРОВЕРКА УСТАНОВКИ

Убедитесь, что всё работает:

```bash
# 1. Проверить версию Python
python3.11 --version
# Вывод: Python 3.11.x

# 2. Активировать окружение
.\venv\Scripts\Activate.ps1  # Windows
# или
source venv/bin/activate     # Linux/macOS

# 3. Проверить зависимости
pip list | grep -i flask
# Вывод должен содержать Flask

# 4. Запустить тест подключения
python -c "from app import create_app; print('OK')"
# Вывод: OK

# 5. Запустить приложение
python run.py
# Вывод должен быть без ошибок
```

---

## 📞 КОНТАКТЫ И ПОМОЩЬ

- **Документация проекта**: Смотрите файлы `.md` в корне репозитория
- **GitHub Issues**: Создавайте issues для проблем
- **Лицензия**: MIT - смотрите [`LICENSE`](LICENSE)

---

## 📝 СТРУКТУРА ПРОЕКТА

```
.
├── app.py                    # Flask приложение
├── config.py                 # Конфигурация
├── models.py                 # Модели базы данных
├── routes.py                 # API endpoints
├── run.py                    # Скрипт запуска (рекомендуется)
├── requirements.txt          # Зависимости
├── .env.example              # Пример переменных окружения
├── Dockerfile                # Docker конфигурация
│
├── services/                 # Слой сервисов
│   ├── github_service.py     # GitHub API
│   ├── pypi_service.py       # PyPI API
│   ├── version_checker.py    # Сравнение версий
│   └── notifier.py           # Уведомления
│
├── templates/                # HTML шаблоны
│   ├── base.html            # Базовый шаблон
│   ├── index.html           # Главная страница
│   ├── projects.html        # Список проектов
│   └── project.html         # Детальная страница
│
├── static/                   # Статические файлы
│   ├── css/style.css        # Стили
│   └── js/main.js           # JavaScript
│
├── tests/                    # Тесты
│   ├── conftest.py          # Конфигурация pytest
│   ├── test_models.py       # Тесты моделей
│   ├── test_services.py     # Тесты сервисов
│   └── test_routes.py       # Тесты API
│
└── docs/                     # Документация
    ├── README.md            # Описание проекта
    ├── INSTALL.md           # Установка
    ├── API_DOCS.md          # API документация
    ├── ARCHITECTURE.md      # Архитектура
    ├── TESTING.md           # Тестирование
    └── PYTHON-313-FIX.md    # Решение Python 3.13
```

---

**Готово! Теперь запустите:**
```bash
python run.py
```

Откройте http://localhost:5000 и начните использовать приложение! 🎉
